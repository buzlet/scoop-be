# scoop-be

Personal Scoop bucket.

## Add bucket

```powershell
scoop bucket add scoop-be https://github.com/buzlet/scoop-be
```

## Install a package

```powershell
scoop install scoop-be/<package>
```

Manifests are stored in `bucket/`.
